# 网页漫画图片提取器方案建议

## 推荐形式
优先做 **Chrome 插件（Manifest V3）**，再配一个可选的本地下载助手（后续）。

原因：
- 你要“从网页上拔图 + 按页面顺序命名 + 下载到本地”，这是浏览器扩展最直接的能力范围。
- 扩展可以直接读取当前标签页 DOM、网络请求、懒加载图片链接。
- 用户体验最好：打开目标网页 -> 点插件按钮 -> 一键抓取预览 -> 下载。

## 功能拆分
1. **内容脚本（content script）**
   - 扫描页面图片候选：`img`、`picture source`、常见懒加载属性（`data-src`, `data-original` 等）。
   - 过滤规则：
     - 最小宽高阈值（例如宽 >= 700，高 >= 1000）
     - 长宽比接近漫画页（例如 0.6~0.85）
     - 排除小图标、头像、广告位。
   - 顺序规则：按页面垂直位置（`getBoundingClientRect().top + scrollY`）从上到下排序。

2. **后台脚本（service worker）**
   - 统一处理下载队列，调用 `chrome.downloads.download`。
   - 命名格式：`{漫画名}_{chapter}_{0001}.jpg`。

3. **弹窗页（popup）**
   - 提供阈值设置（最小宽高、是否去重、命名前缀）。
   - 提供抓取结果预览与“全选下载”。

## 关键技术点
- **去重**：同 URL 去重 + 去 query 参数去重（可选）+ 感知哈希（后续增强）。
- **真实大图获取**：
  - 优先 `currentSrc` / `srcset` 中最高分辨率。
  - 解析 `data-*` 懒加载字段。
- **反爬与跨域**：
  - 多数情况下 `downloads.download({url})` 可直接下。
  - 如果站点做签名或临时 token，需在页面上下文及时抓原始 URL。
- **命名顺序稳定性**：
  - 先收集并排序，再统一命名下载，避免并发导致乱序。

## 为什么不优先做其他形式
- **油猴脚本**：开发更快，但权限/管理与分发不如插件规范，UI 能力较弱。
- **Python 爬虫**：适合批量离线，但对动态渲染页面、登录态、反爬处理成本高。
- **Electron 桌面端**：太重，不适合先验证 MVP。

## 建议的实施路径（MVP）
1. 先做 Chrome 插件 MVP：当前页提取、排序、命名、下载。
2. 再做增强：
   - 自动翻页抓取整章
   - ZIP 打包（需 `JSZip`）
   - 站点规则模板（按域名适配）
3. 最后再考虑跨浏览器（Edge/Firefox）兼容。
